import { AnswerRecord, AREA_LABELS, getSolvingGuide } from "@/lib/quizUtils";
type Props={wrongAnswers:AnswerRecord[];onPick:(index:number)=>void;onHome:()=>void};
export default function WrongAnswerList({wrongAnswers,onPick,onHome}:Props){return <main className="mx-auto min-h-screen w-full max-w-md px-4 py-6"><section className="rounded-[2rem] border border-slate-200 bg-white/95 p-5 shadow-xl shadow-slate-200"><div className="flex items-center justify-between"><h1 className="text-3xl font-black text-slate-900">오답 목록</h1><button onClick={onHome} className="rounded-full bg-slate-100 px-4 py-2 font-black text-slate-600">홈</button></div><p className="mt-3 text-sm font-bold text-slate-500">틀린 문제는 유형을 먼저 보고, 지문에서 근거 문장을 다시 찾으세요.</p><div className="mt-6 grid gap-4">{wrongAnswers.length===0?<div className="rounded-3xl bg-green-50 p-6 text-center font-black text-green-700">오답이 없습니다.</div>:wrongAnswers.map((answer,index)=><button key={answer.question.id} onClick={()=>onPick(index)} className="rounded-3xl border border-slate-200 bg-white p-4 text-left shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><p className="font-black text-violet-700">{index+1}. {AREA_LABELS[answer.question.section]} · {answer.question.questionType}</p><p className="mt-2 text-lg font-black text-slate-900">{answer.question.question}</p><p className="mt-3 text-sm font-bold text-slate-500">{getSolvingGuide(answer.question.questionType)}</p></button>)}</div></section></main>}
